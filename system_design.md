Modules:
1️⃣ preprocessing.py – handles data cleaning, encoding, and splitting
2️⃣ model.py – defines the neural network or ML model structure
3️⃣ train.py – trains the model and saves checkpoints
4️⃣ evaluate.py – loads model and tests performance
